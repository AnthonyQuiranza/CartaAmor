var typed = new Typed('#typed', {
    stringsElement: '#typed-strings',
    cursorChar: '💓',
    typeSpeed: 40,
    backDelay: 3000,


  });

  var typedTitle = new Typed('#typedTitle', {
    stringsElement: '#typedTitleString',
    loop: true,
    typeSpeed: 70,
  });

  new GreenAudioPlayer('.reproductor');